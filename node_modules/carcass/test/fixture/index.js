var carcass = require('carcass');

// Register.
carcass.register(__dirname, 'models');

// Register.
carcass.register(__dirname, 'resources');

// .
exports.start = function(callback) {
    var server = new carcass.servers.Http();
    server.mountAll();
    return server.start(callback);
};
