var model = require('blah').constructors.model.extend({
    lorem: 'ipsum'
});

model.prototype.initialize = function() {
// model.super_.prototype.initialize.apply(this, arguments);
};

module.exports = model;
